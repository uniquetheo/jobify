import Layout from "../../../components/Layout";

const TemplateGalleryPage = () => {
  return (
    <Layout>
      <div>Template Gallery Page</div>
    </Layout>
  );
};

export default TemplateGalleryPage;

// Purpose: Showcase the available CV and cover letter templates for users to choose from.
// Content:
// Grid or list view of template thumbnails with brief descriptions.
// Filters or sorting options (e.g., by style, color, format).
// Select button to choose a template and proceed to preview.
